import * as Styled from './styles'

const Loader = () => {
  return (
    <Styled.LoaderWrapper>
      <Styled.LoaderImg src="/assets/pokeball.png" alt="carregando..." />
    </Styled.LoaderWrapper>
  )
}

export default Loader
